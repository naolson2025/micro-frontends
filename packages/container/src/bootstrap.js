import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// container will render the App component immediately
// no need to check for dev mode or anything
ReactDOM.render(<App />, document.getElementById('root'));